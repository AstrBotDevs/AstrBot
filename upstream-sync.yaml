schema_version: 1

upstream:
  repo: https://github.com/AstrBotDevs/AstrBot.git
  branch: master

last_synced:
  commit: 346b85db9d79207ea7b51694cce5276203612af4
  synced_at_utc: 2026-08-06T11:26:19Z
  sync_method: manual-adaptation
  source_pr: '9553, 346b85db9d79207ea7b51694cce5276203612af4'
  note: >-
    Reviewed the two upstream/master commits after
    bb9b1878474e5ed65667d5c2c6b7fd6efbc51db4 through
    346b85db9d79207ea7b51694cce5276203612af4. The marker is not an ancestor
    of this fork branch, so no upstream commit was merged or mechanically
    cherry-picked. Fork-native port: adapted the platform-selection tooltips
    from #9553 to the current Vue item type and shipped zh-CN/en-US strings.
    The upstream ru-RU resource is excluded because this fork supports only
    zh-CN and en-US. Ported the platform-template ordering while retaining the
    fork's existing labels, loopback callback defaults, and OneBot fields,
    with a regression test for that invariant. Followed the absorbed feature
    set as fork-owned 4.27.2 metadata, uv.lock revision, bilingual changelog,
    and platform-start documentation; the upstream release text was not
    copied. Skipped the RainYun guide because the fork deliberately removed
    vendor promotion, does not publish that prebuilt application, and requires
    source-build and explicit secure remote-access guidance.
