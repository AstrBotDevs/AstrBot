## [4.27.5] - [WIP]

### Changed

- Split the main SQLite facade into protocol-aligned mixins. `SQLiteDatabase`
  still composes every domain store, but table models, schema initialization,
  and queries now live under `astrbot/core/db/po/`, `schema.py`, and
  `stores/`.
- Make SQLModel tables the only main-database schema source of truth. Startup
  registers the explicit model registry, runs `create_all`, and applies SQLite
  PRAGMAs. It no longer probes `PRAGMA table_info` or patches columns and
  partial unique indexes onto an existing file.
- **Breaking:** rebuild the main database from the current models. Stop the
  process, delete `data/data_v4.db*` under the runtime root, and start again.
  Conversations, long-term memory, authorization bindings, API keys, and other
  main-database rows are not migrated. Knowledge-base files are unchanged.
- **Breaking:** command identity is `command_id` only
  (`{plugin}:{original path}` with spaces as dots). `alter_cmd` is not migrated
  from Python method names or historical short names such as `plugin_ls` or
  `op`. Unmatched `command_configs` rows are deleted. Built-in commands keep
  declared names and aliases unless Dashboard stored `manual_rename`. Former
  invocation names such as `/plugin ls` and `/reset` are not aliases.
- Drop unused `keep_original_alias` from `command_configs` in a separate
  startup transaction. A drop failure does not roll back schema creation.
- Persist authorization bindings and capabilities as one current-state row per
  normalized scope. Revoke or expiry followed by a new grant revives the
  original row; history stays in `AuthAuditLog`.
- Persist non-null scope keys. Global role bindings use `GLOBAL_SCOPE_ID`;
  all-config capabilities use `ANY_CONFIG_SCOPE_ID`. Uniqueness no longer
  depends on SQLite treating multiple `NULL` values as distinct.
- Make `ApiKeyStore` the sole `AuthCapability` writer. Key create/revoke and
  derived capabilities share one transaction; `grant_capability()` delegates
  to `upsert_capability()`.
- Move runtime `Conversation` to `astrbot/core/conversation_models.py` and
  `Personality` to `astrbot/core/persona_runtime/models.py`.
  `astrbot.core.db.po` no longer exports those names. The plugin SDK still
  exports `Personality` from `astrbot.api.provider`.

### Fixed

- ChatUI nested Transport and Language menus open on click on coarse-pointer
  devices instead of hover. (#9782, fork adaptation)
- Anthropic `api_base` no longer requires a `/v1` suffix; trailing `/v1` is
  stripped at runtime so saved Anthropic endpoints match OpenAI-style bases.
  (#9802, fork adaptation)
- Command management shows commands from deactivated plugins as inactive, keeps
  the stored enabled flag unchanged, blocks enable/disable and rename, and
  excludes those commands from conflict detection. (#9664, fork adaptation)
- Console Loguru output and the root logger follow the configured `log_level`.
  (#9805, fork adaptation)
- DingTalk QR app creation treats `CREATING` as pending instead of an unknown
  status. (#9808, fork adaptation)
- OpenAI Chat Completions tool schemas are emitted in stable name order without
  mutating ToolSet insertion order. (#9798, fork adaptation)

### Documentation

- Folded the implemented main-database layout, protocol map, authorization
  persistence rules, and empty-database cutover into the bilingual architecture,
  development, FAQ, and long-term-memory pages. Removed the temporary
  planning documents.
- Documented stable `command_id` identity, the unmatched-row cutoff, and that
  former built-in short names are not aliases.
- Documented deactivated-plugin command rows, Anthropic API Base `/v1`
  stripping, ChatUI touch nested menus, `log_level` console behavior, and
  DingTalk `CREATING` polling.

### Maintenance

- Split store tests under `tests/unit/db/` and lock the 35-table schema,
  WAL/`busy_timeout`, and idempotent initialization in `test_schema.py`.
- Bumped the CodeQL GitHub Action pin to 4.37.8 with an immutable SHA.
  (#9799, fork adaptation)

### Fork Adaptations

- Resolved plugin activation through `PluginRegistry` instead of `star_map`,
  and kept `CommandItem` on the Dashboard domain contract plus OpenAPI.
- Replaced the console log sink only after a successful add and kept
  `_SafeConsoleStream`.
- Sorted `openai_chat_completions_schema` rather than the removed
  `openai_schema` helper.

### Fork Deviations

- Did not replay the upstream Dockerfile `COPY` reorder (#9787): this fork
  already copies manifests, installs dependencies, then copies the source tree.
- Did not replay the WeCom receive-loop offload (#9759): `_start_message_task`
  already keeps the command ACK path responsive.

## 中文

### 变更

- 将主 SQLite 门面拆成按域协议对齐的 mixin。`SQLiteDatabase` 仍组合全部域存储，
  但表模型、schema 初始化和查询分别位于 `astrbot/core/db/po/`、`schema.py` 和
  `stores/`。
- 以 SQLModel 表作为主库 schema 的唯一真源。启动时显式注册模型、执行 `create_all`
  并设置 SQLite PRAGMA；不再探测 `PRAGMA table_info`，也不再给已有文件补列或
  部分唯一索引。
- **破坏性变更：** 按当前模型重建主库。停进程后删除 runtime root 下的
  `data/data_v4.db*` 再启动。会话、长期记忆、授权绑定、API Key 等主库记录不会
  从旧文件迁出。知识库文件不受影响。
- **破坏性变更：** 指令身份只认 `command_id`（`{plugin}:{original path}`，空格
  换成点）。`alter_cmd` 不从 Python 方法名或历史短名（如 `plugin_ls`、`op`）
  迁移。无法认领的 `command_configs` 行会删除。未在 Dashboard 手动重命名的内置
  指令保持当前声明名和别名。`/plugin ls`、`/reset` 等旧调用名不是别名。
- 启动时在独立事务中删除未使用的 `command_configs.keep_original_alias` 列；
  删除失败不影响 schema 创建。
- 授权绑定和 capability 按规范化作用域只保留一行当前状态；撤销或过期后重新授权
  会恢复原行，历史写入 `AuthAuditLog`。
- 持久化非空 scope key。角色绑定的全局作用域使用 `GLOBAL_SCOPE_ID`；适用于所有
  配置的 capability 使用 `ANY_CONFIG_SCOPE_ID`。唯一性不再依赖 SQLite 把多个
  `NULL` 当作互不相等。
- `ApiKeyStore` 成为 `AuthCapability` 的唯一写入者。创建/撤销 key 与派生
  capability 在同一事务中完成；`grant_capability()` 委托 `upsert_capability()`。
- 将运行时 `Conversation` 移到 `astrbot/core/conversation_models.py`，将
  `Personality` 移到 `astrbot/core/persona_runtime/models.py`。
  `astrbot.core.db.po` 不再导出这两个名字。插件 SDK 仍从 `astrbot.api.provider`
  导出 `Personality`。

### 修复

- 粗指针（触摸）设备上，ChatUI 传输模式和语言二级菜单改为点击打开，不再依赖悬停。
  (#9782，fork 适配)
- Anthropic `api_base` 不再要求 `/v1` 后缀；运行时会去掉末尾 `/v1`，已保存地址
  可继续使用。 (#9802，fork 适配)
- 指令管理将所属插件未启用的指令显示为未启用，不改存储的启用标志，禁止启用、停用
  和重命名，并且这些指令不参与冲突检测。 (#9664，fork 适配)
- 控制台 Loguru 输出和根 logger 跟随配置的 `log_level`。 (#9805，fork 适配)
- 钉钉扫码创建应用时把 `CREATING` 视为等待中，而不再当作未知状态。 (#9808，fork 适配)
- OpenAI Chat Completions 工具 schema 按工具名稳定排序，不改变 ToolSet 插入顺序。
  (#9798，fork 适配)

### 文档

- 把已落地的主库布局、协议对照、授权持久化规则和空库切库约定写入中英文架构、
  开发、FAQ 和长期记忆文档，并删除临时计划页。
- 补充稳定 `command_id`、无法认领行删除，以及旧内置短名不是别名的说明。
- 补充未启用插件指令行、Anthropic API Base 去 `/v1`、ChatUI 触摸二级菜单、
  `log_level` 控制台行为，以及钉钉 `CREATING` 轮询说明。

### 维护

- 将存储测试按协议拆到 `tests/unit/db/`，并在 `test_schema.py` 中锁定 35 张表、
  WAL/`busy_timeout` 以及幂等初始化。
- 将 CodeQL GitHub Action 钉到 4.37.8 的不可变 SHA。 (#9799，fork 适配)

### Fork 适配

- 通过 `PluginRegistry` 读取插件启用状态，而不是 `star_map`；`CommandItem` 仍在
  Dashboard 域契约和 OpenAPI 中维护。
- 控制台 sink 仅在新增成功后再移除旧 sink，并保留 `_SafeConsoleStream`。
- 对 `openai_chat_completions_schema` 排序，而不是已删除的 `openai_schema`。

### Fork 偏差

- 未回放上游 Dockerfile `COPY` 重排 (#9787)：本 fork 已先复制清单、安装依赖，
  再复制源码树。
- 未回放企业微信接收循环卸载 (#9759)：`_start_message_task` 已保证命令 ACK 路径
  可响应。
